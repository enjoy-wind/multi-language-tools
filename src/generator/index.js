import { combineConfig } from "./config/index.js";

const generator = (transTokens) => {
  const { transFnStr } = combineConfig;
};
/*发现历史Key*/
const findHistoryKey = (name) => {};
/*自动生成Key*/
const autoGeneratorKey = () => {};

export { generator };
